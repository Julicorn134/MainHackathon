# Copy to secrets.toml in this directory. The real file is ignored by Git.
AI_PROVIDER = "openrouter"
OPENROUTER_API_KEY = ""
OPENROUTER_MODEL = "openai/gpt-4.1-mini"

# Optional alternative: set AI_PROVIDER = "openai" to call OpenAI directly.
OPENAI_API_KEY = ""
OPENAI_MODEL = "gpt-4.1-mini"

# Cloud uploads. Copy the server secret from Supabase > Settings > API Keys.
# Keep this file's real values only in secrets.toml / your deployment's secret store.
BLOODSIGHT_DATA_BACKEND = "supabase" # use "sqlite" for offline tests
SUPABASE_URL = "https://xbabvlorkjilnnxrifmy.supabase.co"
SUPABASE_SECRET_KEY = ""
# Legacy SUPABASE_SERVICE_ROLE_KEY is also accepted; a publishable/anon key is not.

# Server-only SMS. Trial mode uses Twilio's predefined account-alert template.
TWILIO_ACCOUNT_SID = ""
TWILIO_AUTH_TOKEN = ""
TWILIO_MESSAGE_MODE = "trial_template"
# For custom mode, use an SMS-enabled sender or a Messaging Service with a sender pool.
TWILIO_FROM_NUMBER = ""
TWILIO_MESSAGING_SERVICE_SID = ""
